import * as grpc from 'grpc';
import { GRPCOpts } from './common';
export interface GetHealthCheckOpts {
    timeoutInMS: number;
    grpcCredentials: grpc.ChannelCredentials;
    grpcOpts: GRPCOpts;
}
export declare function getBrakeHealthCheckFunc(service: string, host: string, opts: GetHealthCheckOpts): () => void;
